spring.application.name=Happenhub

# MongoDB connection
spring.data.mongodb.uri=mongodb://localhost:27017/happenhub
spring.data.mongodb.database=happenhub

# Server configuration
server.port=8080

# JWT Configuration
jwt.secret=OQwh9iPXK9hQnSmjCveSRLhJxBY0gRFZAnteZctLOmhqGRSz3WJ0
jwt.expiration=3600000

# CORS Configuration (already handled in WebConfig.java)
spring.mvc.cors.allowed-origins=http://localhost:5173
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE
spring.mvc.cors.allowed-headers=*

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging Configuration
logging.level.com.happenhub=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
